import React from "react";
import styles from "./BurnLink.module.css";

const BurnLink = () => {
  return (
    <div className={styles.BurnLink}>
      <button>Generate Your Link</button>
    </div>
  );
};

export default BurnLink;
